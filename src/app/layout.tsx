// Root layout — locale layout (src/app/[locale]/layout.tsx) handles
// html/body, lang attribute, fonts, and providers.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
