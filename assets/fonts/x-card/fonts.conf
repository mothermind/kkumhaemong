<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Embedded fonts for x-card rendering — no system fonts required -->
  <dir>/Users/llnormll/WorkSpace/my-fortune-site/assets/fonts/x-card</dir>
  <!-- In CI/Docker, override this path via XCARD_FONT_DIR env var (see render-x-card.mjs) -->
  <cachedir>/tmp/fontconfig-xcards-cache</cachedir>
  <config>
    <rescan><int>30</int></rescan>
  </config>
</fontconfig>
